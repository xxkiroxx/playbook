---
- name: Install apache httpd
  hosts: 192.168.1.20
  remote_user: root
  become: true
  vars:
       mach: prod
  tasks:
   - name: install apache
     yum:
         name: httpd
         state: latest
   - name: Enable service
     service:
             name: httpd
             state: started
